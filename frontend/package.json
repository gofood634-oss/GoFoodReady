{
  "name": "gofood-frontend",
  "version": "1.0.0",
  "description": "GoFood - Frontend static files",
  "scripts": {
    "start": "npx http-server -p 8000 -c-1",
    "serve": "npx http-server -p 8000 -c-1",
    "dev": "npx http-server -p 8000 -c-1 --cors",
    "check-port": "node -e \"const net=require('net');const server=net.createServer();server.listen(8000,()=>{console.log('Port 8000 is available');server.close();}).on('error',(err)=>{console.log('Port 8000 is in use:',err.message);process.exit(1);})\""
  },
  "keywords": [
    "food-delivery",
    "frontend",
    "static"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}